<h1>Add a New Cat</h1>
<%= simple_form_for @cat, url: instigator_cats_path do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <br />
    <%= f.submit "Create", class: 'btn btn-primary' %>
<% end %>